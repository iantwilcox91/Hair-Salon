<h1>Clients</h1>
@if (@Model.Count == 0)
  {
    <p>No Clients Added</p>
  }
else
{
  <ol>
    @foreach (var Client in Model)
    {
      <li>@Client.GetName()</li>
    }
  </ol>
}

<p><a href="/deleteAllClients">Delete all Clients</a></p>

<p><a href="/">Home</a></p>
